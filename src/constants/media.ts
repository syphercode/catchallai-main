// JPEG quality factor used when encoding cropped/transformed images.
// 0.92 (92%) balances visual fidelity against file size.
export const JPEG_QUALITY = 0.92;

// Tilt slider range and step for the image crop panel (degrees).
export const TILT_MIN = -45;
export const TILT_MAX = 45;
export const TILT_STEP = 0.5;
